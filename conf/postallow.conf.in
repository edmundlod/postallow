# CONFIGURATION OPTIONS FOR POSTALLOW
# https://github.com/edmundlod/postallow

# FILE PATHS

# Package-managed default sender lists — updated on upgrade, do not edit
allowlist_hosts=@DATADIR@/allowlist_hosts

# User additions — safe to edit, preserved across upgrades
custom_hosts_file=@SYSCONFDIR@/postallow/custom_hosts

# Location of spf-tools
spftoolspath=@BINDIR@/spf-tools

# Directory where the generated CIDR files are written.
# Postallow writes these files as the postallow user (world-readable, mode 644).
# Point Postfix's postscreen_access_list at this directory directly — no need
# for files to live under /etc/postfix.
output_dir=@OUTPUTDIR@


# OTHER OPTIONS

# Include additional Yahoo/Verizon/AOL outbound IPs scraped from
# senders.yahooinc.com (on top of those already in Yahoo's SPF records).
# Run scrape_yahoo periodically to keep the list fresh.
include_yahoo="no"

# Build a blocklist as well?
# List domains to block in the blocklist_hosts section of allowlist_hosts.
enable_blocklist=no
